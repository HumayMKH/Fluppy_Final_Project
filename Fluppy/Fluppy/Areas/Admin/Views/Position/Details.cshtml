@model Fluppy.Models.Position

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

    <div class="ml-4 mt-2">
        <h4>Position</h4>

        <h2 style="font-weight:bold;">Details</h2>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

        </dl>
        <p>
            <a href="@Url.Action("Update", "Position", new { id = Model.Id })" class="btn btn-info  mb-2">Update</a>

            <a href="@Url.Action("Index", "Position")" class="btn btn-danger  mb-2 ml-2">Back To List</a>
        </p>
    </div>